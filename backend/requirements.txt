# FastAPI backend for Animal Species Recognition System
# Python 3.11 required (tensorflow 2.16.1 supports Python 3.9-3.12)

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
Pillow==10.4.0
numpy>=1.26.4,<2.0.0
opencv-python-headless==4.10.0.84

# Deep learning — TensorFlow + Keras (EfficientNet-B0)
# tensorflow (CPU-only on Render, no GPU available)
tensorflow==2.16.1
